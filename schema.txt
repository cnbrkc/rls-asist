# 📐 RLS-ASİST (otoXtra) — PROJE ŞEMASI v2.0

## 🗂️ 1. GENEL BAKIŞ

| Alan | Detay |
|------|-------|
| **Proje Adı** | otoXtra (RLR-ASİST) |
| **Amaç** | Otomobil temalı Instagram Reels + Threads içeriği otomatik üretimi |
| **Teknoloji** | Python + Streamlit + Google Gemini AI |
| **Çalışma Ortamı** | GitHub Codespaces / Dev Container |
| **Dil** | Python 3.11 |
| **Toplam Dosya** | 13 dosya + 1 klasör |

---

## 📁 2. DOSYA AĞACI

```
rls-asist/
├── .devcontainer/
│   └── devcontainer.json        → Codespaces yapılandırması
├── app.py                       → ANA UYGULAMA (Streamlit UI, ~418 satır)
├── router.py                    → SmartRouter (API rotasyonu, ban yönetimi)
├── prompts.py                   → Prompt yükleme ve dinamik şablon üretimi
├── storage.py                   → JSON kayıt CRUD işlemleri
├── config.py                    → Sabitler, API key'leri, model listeleri
├── utils.py                     → Saf yardımcı fonksiyonlar
├── telegram.py                  → Telegram Bot API gönderimleri
├── kurallar.txt                 → Reels içerik üretim kuralları (prompt)
├── sistem_talimati.txt          → Sistem talimatı şablonu (dinamik)
├── guncellik_talimati.txt       → Güncellik/doğruluk kuralları (dinamik)
├── video_analiz_promptu.txt     → Video analiz promptu (dinamik)
├── threads_promptu.txt          → Threads üretim promptu (statik)
├── requirements.txt             → Python bağımlılıkları
├── README.md                    → Proje başlığı (minimal)
└── kayitlar.json                → [RUNTIME] Üretim geçmişi (otomatik oluşur)
```

---

## 📄 3. DOSYA DETAYLARI

---

### 3.1 — `app.py` (ANA UYGULAMA)

**Boyut:** ~418 satır  
**Framework:** Streamlit  
**Sorumluluk:** UI + Session State + Pipeline koordinasyonu

#### Bölüm Haritası (Sırayla):

| Satır Aralığı | Bölüm | Açıklama |
|---|---|---|
| 1–22 | **Import'lar** | streamlit, json, datetime, config, storage, prompts, utils, telegram, router |
| 23–42 | **SAYFA AYARLARI + INIT** | page_config, session_state başlatma, router init |
| 43–100 | **SOL MENÜ (Sidebar)** | Ses seçimi (11 opsiyon), API durumu, Telegram bağlılık, geçmiş üretimler |
| 101–145 | **ANA ARAYÜZ** | Video upload, analiz notları, üretim notları, süre slider, ton seçimi, ÜRET butonu |
| 146–235 | **ADIM 1-2: ANALİZ + METİN** | Video analiz → Metin üretimi (JSON schema) |
| 236–270 | **ADIM 3: THREADS** | Threads metni üretimi (fallback ile) |
| 271–313 | **ADIM 4: SES + KAYIT** | TTS → WAV dosyası, kayitlar.json'a ekleme |
| 314–418 | **SONUÇLAR UI** | Medya oynatma, metin gösterimi, Telegram gönderimi, yeniden ses üretimi, CoT expander |

#### Session State Anahtarları:

| Key | Tip | Açıklama |
|-----|-----|----------|
| `sonuc` | dict/None | Son üretim sonucu |
| `log_satirlari` | list[str] | Canlı log |
| `gecici_ses_dosyalari` | list[str] | Temp WAV yolları |

#### Sidebar Ses Seçenekleri (11 ses):

| Ses | Karakter |
|-----|----------|
| Autonoe | Parlak - Kadın |
| Puck | Enerjik - Erkek |
| Aoede | Yumuşak - Kadın |
| Callirrhoe | Doğal - Kadın |
| Kore | Net - Kadın |
| Leda | Dinamik - Kadın |
| Zephyr | Parlak - Kadın |
| Charon | Bilgi - Erkek |
| Orus | Sert - Erkek |
| Iapetus | Akıcı - Erkek |
| Umbriel | Rahat - Erkek |

#### Ton Seçenekleri:

| Ton | Bilgi/Eğlence Oranı |
|-----|---------------------|
| 🎭 Eğlence Ağırlıklı | 1/4 bilgi, 3/4 eğlence |
| ⚖️ Dengeli (varsayılan) | 1/2 bilgi, 1/2 eğlence |
| 🧠 Bilgi Ağırlıklı | 3/4 bilgi, 1/4 eğlence |
| 📊 Teknik Odaklı | 9/10 bilgi, 1/10 eğlence |

---

### 3.2 — `router.py` (SmartRouter)

**Boyut:** ~199 satır  
**Sorumluluk:** API key rotasyonu, ban yönetimi, model fallback, hata handling

#### SmartRouter Sınıfı Detayı:

```
SmartRouter
├── __init__()              → blacklist dict başlatma (session_state)
├── _is_banned()            → mail+model kombinasyonu banlı mı?
├── _ban()                  → Ban ekle (scope: model/key/combo)
├── _retry_delay_cikar()    → Hata mesajından retryDelay parse et
├── _parse_hata()           → Hata tipini belirle (quota/404/503/free_tier)
├── _handle_hata()          → Hata aksiyonu (break_model / devam)
├── _make_request()         → Model×Key matrisinde döngü, ilk başarılıyı döndür
├── metin_uret()            → JSON schema ile structured output + opsiyonel Google Search
├── ses_uret()              → TTS (text-to-speech) → WAV dosyası
└── video_analiz_et()       → Video bytes + prompt → analiz metni
```

#### Model Stratejisi:

| Görev | Modeller | Öncelik |
|-------|----------|---------|
| Video Analiz | `gemini-3.6-flash` → `gemini-2.5-flash` | En zeki modeller |
| Metin Üretimi | `gemini-3.6-flash` → `gemini-2.5-flash` → `gemini-3.5-flash-lite` → `gemini-3.1-flash-lite` | Zeki + yüksek kota yedekler |
| Ses (TTS) | `gemini-2.5-flash-preview-tts` | Tek model |

#### Cooldown/Ban Sistemi:

| Hata Tipi | Cooldown | Scope |
|-----------|----------|-------|
| Free tier yok (limit: 0) | 7 gün | Model bazlı (tüm key'ler) |
| 429 / Quota | retryDelay veya 60sn | Key+Model combo |
| 503 / Unavailable | 15 dk | Key+Model combo |
| 404 / Not found | 24 saat | Model bazlı (tüm key'ler) |
| Diğer | 5 dk | Key+Model combo |

---

### 3.3 — `prompts.py` (Prompt Yönetimi)

**Boyut:** ~52 satır  
**Sorumluluk:** `.txt` dosyalarını okuma, placeholder'ları doldurma

#### Fonksiyonlar:

| Fonksiyon | Açıklama |
|-----------|----------|
| `prompt_dosyasi_oku(dosya_adi)` | Belirtilen .txt dosyasını okur |
| `guncellik_talimatini_hazirla()` | `guncellik_talimati.txt` → `{bugunun_tarihi}` placeholder'ını doldurur |
| `sistem_talimatini_hazirla(sure, kelime, oran)` | 6 placeholder'ı doldurur: `{guncellik_talimati}`, `{sure_saniye}`, `{kelime_sayisi}`, `{min_kelime}`, `{max_kelime}`, `{bilgi_orani}` |
| `video_analiz_promptunu_hazirla(notlar, sure)` | 3 placeholder'ı doldurur: `{ek_notlar_bolumu}`, `{guncellik_talimati}`, `{sure_saniye}` |

---

### 3.4 — `storage.py` (Kayıt Yönetimi)

**Boyut:** ~47 satır  
**Sorumluluk:** `kayitlar.json` CRUD işlemleri

#### Fonksiyonlar:

| Fonksiyon | Açıklama |
|-----------|----------|
| `kayitlari_yukle()` | JSON dosyasını okur, liste döner |
| `kayitlari_kaydet(kayitlar)` | Listeyi JSON'a yazar |
| `eski_sesleri_temizle(kayitlar)` | 24 saatten eski ses dosyalarını siler |
| `kayit_ekle(veri)` | Yeni kayıt ekler, max 5 kayıt tutar, eski sesleri temizler |

---

### 3.5 — `config.py` (Yapılandırma)

**Boyut:** ~60 satır  
**Sorumluluk:** Sabitler, API key'leri, model listeleri, tarih yardımcıları

#### Yapılandırma Öğeleri:

| Kategori | Öğeler |
|----------|--------|
| **API Keys** | `GEMINI_KEYS` (dict), `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID` |
| **Cooldowns** | `COOLDOWN_QUOTA=60`, `COOLDOWN_UNAVAILABLE=900`, `COOLDOWN_GENERIC=300`, `COOLDOWN_NOT_FOUND=86400`, `COOLDOWN_FREE_TIER_YOK=604800` |
| **Modeller** | `VIDEO_ANALIZ_MODELLERI`, `METIN_MODELLERI`, `SES_MODELI` |
| **Kayıt** | `KAYIT_DOSYASI="kayitlar.json"`, `MAX_KAYIT=5`, `SES_DOSYA_OMRU_SAAT=24` |
| **Tarih** | `AYLAR_TR`, `GUNLER_TR`, `guncel_tarih_metni()` |

---

### 3.6 — `utils.py` (Yardımcı Fonksiyonlar)

**Boyut:** ~50 satır  
**Sorumluluk:** Saf fonksiyonlar (stateless)

#### Fonksiyonlar:

| Fonksiyon | Açıklama |
|-----------|----------|
| `markdown_temizle(metin)` | Markdown code block'ları temizler |
| `json_parse(metin)` | JSON string'i parse eder, fallback ile |
| `gecici_dosya_olustur(veri, uzanti)` | Temp dizinine UUID'li dosya yazar |
| `sekmeyi_aktif_tut()` | Streamlit sekmesini aktif tutmak için JS inject eder |

---

### 3.7 — `telegram.py` (Telegram Bot API)

**Boyut:** ~110 satır  
**Sorumluluk:** Telegram'a metin ve ses gönderimi

#### Fonksiyonlar:

| Fonksiyon | Açıklama |
|-----------|----------|
| `telegram_mesaj_gonder(bot_token, chat_id, metin)` | Text mesaj gönderir |
| `telegram_ses_gonder(bot_token, chat_id, dosya_yolu, baslik)` | WAV dosyası gönderir |
| `telegram_toplu_gonder(bot_token, chat_id, veri, ses_dosyasi, log_ekle)` | Tüm çıktıları sırayla gönderir (ses metni, reels açıklaması, kapak başlıkları, threads, ses dosyası) |

---

### 3.8 — `kurallar.txt` (STATİK PROMPT)

**Rol:** Ana içerik üretim kural seti (system prompt'un ilk parçası)  
**Boyut:** ~250 satır  
**Dinamik mi?:** HAYIR (sabit metin)

#### İçerik Bölümleri:

| Bölüm | Konu |
|-------|------|
| §1 KİMLİK | otoXtra persona, hedef kitle, ton (samimi halk ağzı) |
| §2 GİRDİ VE HESAPLAMA | Kelime formülü: süre × 2.8 → en yakın 5'in katı, ±%10 tolerans |
| §3 ÇIKTI FORMATI | 4 bölüm sırası: Seslendirme → Açıklama → Hashtag → Kapak |
| §4 SESLENDİRME KURALLARI | Doğal akış, cümle yapısı, bilgi yoğunluğu, duygu/ton, pozisyon alma, TTS format, paylaşılabilirlik, yasaklar |
| §5 CAPTION KURALLARI | 5 katman (Hook → Bağlam → İlişkilendirme → Hot Take → CTA), SEO, biçim |
| §6 HASHTAG KURALLARI | Tam 5 etiket, dağılım (marka/geniş/niş/özel), yasaklı etiketler |
| §7 ENGAGEMENT BAIT YASAĞI | Kırmızı/Gri/Yeşil liste |
| §8 KAPAK BAŞLIK KURALLARI | Ana (2-4 kelime BÜYÜK) + Alt (5-10 kelime), 5 alternatif |
| §9 GENEL İLKELER | Niş sınırı, içerik tipleri |

---

### 3.9 — `sistem_talimati.txt` (DİNAMİK ŞABLON)

**Rol:** System prompt'un ikinci parçası (kurallar.txt'den sonra eklenir)  
**Dinamik mi?:** EVET — 6 placeholder var

#### Placeholder'lar:

| Placeholder | Kaynak | Örnek Değer |
|-------------|--------|-------------|
| `{guncellik_talimati}` | `guncellik_talimati.txt` işlenmiş hali | "Bugün: 27 Temmuz 2026..." |
| `{sure_saniye}` | Kullanıcı input | `30` |
| `{kelime_sayisi}` | Hesaplama: round(30×2.8/5)×5 | `85` |
| `{min_kelime}` | max(5, int(85×0.9)) | `76` |
| `{max_kelime}` | int(85×1.1) | `93` |
| `{bilgi_orani}` | Ton seçimine göre | "Her 2 cümleden 1'i..." |

#### İçerik Bölümleri:

| § | Konu |
|---|------|
| 1 | Süre ve kelime sayısı (KRİTİK) |
| 2 | Bilgi-eğlence oranı (tona göre) |
| 3 | Kullanıcı notları önceliği |
| 4 | Zorunlu düşünce zinciri (CoT) — beyin_firtinasi, veri_kilitleme, oz_elestiri |
| 5 | Çıktı formatı (JSON şema uyumu) |

---

### 3.10 — `guncellik_talimati.txt` (DİNAMİK ŞABLON)

**Rol:** Güncel veri doğrulama kuralları  
**Dinamik mi?:** EVET — 1 placeholder

#### Placeholder:

| Placeholder | Kaynak |
|-------------|--------|
| `{bugunun_tarihi}` | `guncel_tarih_metni()` → "27 Temmuz 2026" |

#### İçerik:
- Google Search ile güncel veri doğrulama zorunluluğu
- Model adı kesinliği (benzer model tuzağı)
- Türkiye pazarı kontrolü (satış var mı?)
- Rakam uydurma yasağı

---

### 3.11 — `video_analiz_promptu.txt` (DİNAMİK ŞABLON)

**Rol:** Video analiz aşamasının promptu  
**Dinamik mi?:** EVET — 3 placeholder

#### Placeholder'lar:

| Placeholder | Kaynak |
|-------------|--------|
| `{ek_notlar_bolumu}` | Kullanıcının analiz notları (varsa) |
| `{guncellik_talimati}` | İşlenmiş güncellik talimatı |
| `{sure_saniye}` | Hedef süre |

#### İçerik (7 Çıktı Bölümü):

| # | Bölüm | Amaç |
|---|-------|------|
| 1 | Görsel Akış Haritası | Açılış/Orta/Kapanış görselleri |
| 2 | Viral Detaylar + Türk İzleyici Kancası | Google Search verileri |
| 3 | Tespit Edilen Teknik Veriler | Marka/model/rakamlar |
| 4 | Kurgu & Hızlandırma Stratejisi | En vurucu anlar |
| 5 | Hook Önerisi | İlk 3 saniye kancası |
| 6 | Kanıştırıcı Kapanış | Son 3 saniye CTA |
| 7 | Duygu Akışı | Görsel-duygu eşlemesi |

#### Kritik Kurallar:
- Marka/model tespiti hiyerarşisi: Kullanıcı > Görsel tespit > "araç"
- Benzer model tuzağı (KIRMIZI ÇİZGİ)
- Türkiye satış kontrolü
- Halüsinasyon yasağı

---

### 3.12 — `threads_promptu.txt` (STATİK PROMPT)

**Rol:** Threads/X paylaşım metni üretimi  
**Dinamik mi?:** HAYIR  
**Boyut:** ~8 satır (çok kısa)

#### Kurallar:
- Sadece JSON üret
- Max 500 karakter
- Hashtag YASAK
- Ton: Ukala ama haklı, "hot take"
- İlk cümle: kesin yargı
- Bitiş: iddialı cümle (soru değil)

---

### 3.13 — `requirements.txt`

```
streamlit
google-genai
requests
```

3 bağımlılık. Tüm diğer modüller (json, os, re, wave, tempfile, uuid, base64) Python stdlib.

---

### 3.14 — `.devcontainer/devcontainer.json`

| Alan | Değer |
|------|-------|
| Image | `mcr.microsoft.com/devcontainers/python:1-3.11-bookworm` |
| Açılacak dosyalar | README.md, app.py |
| VS Code eklentileri | ms-python.python, ms-python.vscode-pylance |
| Post-create | `pip install -r requirements.txt` |

---

### 3.15 — `README.md`

Sadece başlık: `# RLR-ASİST`

---

## 🔄 4. VERİ AKIŞ DİYAGRAMI

```
KULLANICI GİRDİLERİ
├── Video dosyası (opsiyonel, max 20MB)
├── Analiz Notları (text)
├── Üretim Notları (text)
├── Hedef Süre (5-180 sn)
├── İçerik Tonu (4 seçenek)
└── Ses Seçimi (11 seçenek)
         │
         ▼
┌─────────────────────┐
│  PROMPT BİRLEŞTİRME │
│  kurallar.txt       │──→ system_prompt (ADIM 2)
│  + sistem_talimati  │
│  + guncellik        │
└─────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────┐
│  ADIM 1: VIDEO ANALİZ (gemini-3.6/2.5-flash)   │
│  + Google Search aktif                          │
│  → analiz_metni                                 │
└─────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────┐
│  ADIM 2: METİN ÜRETİMİ (structured JSON)        │
│  + Google Search KAPALI                         │
│  → 7 alanlı JSON (CoT + içerik)                 │
└─────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────┐
│  ADIM 3: THREADS (ayrı model çağrısı)           │
│  → threads_aciklamasi (max 500 char)            │
└─────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────┐
│  ADIM 4: TTS (gemini-2.5-flash-preview-tts)    │
│  → WAV dosyası (mono/16bit/24kHz)              │
└─────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────┐
│  ÇIKTI & KAYIT                                  │
│  ├── UI gösterim (ses, metinler, CoT)           │
│  ├── kayitlar.json (son 5 üretim)               │
│  └── Yeniden ses üretimi (düzenlenebilir)       │
│  └── Telegram toplu gönderim (opsiyonel)        │
└─────────────────────────────────────────────────┘
```

---

## 🔐 5. GİZLİ YAPILANDIRMA

`st.secrets["GEMINI_KEYS"]` → Streamlit secrets (`.streamlit/secrets.toml`)

```toml
[GEMINI_KEYS]
"mail1@gmail.com" = "AIza..."
"mail2@gmail.com" = "AIza..."

TELEGRAM_BOT_TOKEN = "bot_token_here"
TELEGRAM_CHAT_ID = "chat_id_here"
```

Birden fazla API key destekleniyor. SmartRouter hepsini sırayla deniyor.

---

## ⚙️ 6. ÇALIŞTIRMA

```bash
streamlit run app.py
```

veya GitHub Codespaces'te otomatik (devcontainer).

---

## 📊 7. MİMARİ NOTLAR / GÖZLEMLER

| # | Gözlem |
|---|--------|
| 1 | **Modüler yapı** — İş mantığı 6 Python dosyasına bölünmüş (app, router, prompts, storage, config, utils, telegram) |
| 2 | **Prompt'lar dosyalarda** — Değişiklik kolay, kod dokunmadan ayarlanabilir |
| 3 | **SmartRouter** — Multi-key × multi-model fallback + akıllı ban sistemi |
| 4 | **Chain of Thought** — JSON schema içine düşünme adımları gömülü (beyin_firtinasi, veri_kilitleme, oz_elestiri) |
| 5 | **Google Search** — Sadece video analiz adımında aktif, metin üretiminde kapalı |
| 6 | **Kalıcı depolama yok** — Sadece `kayitlar.json` (max 5 kayıt), veritabanı yok |
| 7 | **Ses dosyaları geçici** — temp dizinde, 24 saat sonra otomatik silinir |
| 8 | **Tek kullanıcı** — Auth yok, secrets ile API key yönetimi |
| 9 | **Telegram entegrasyonu** — Tüm çıktılar tek tıkla Telegram'a gönderilebilir |
| 10 | **Kelime hesaplama** — Formül: `round(sure × 2.8 / 5) × 5` (±%10 tolerans) |

---

## 📋 8. JSON RESPONSE SCHEMAS

### Metin Üretimi Schema:

```json
{
  "type": "OBJECT",
  "properties": {
    "beyin_firtinasi":    "STRING  → Strateji planı (CoT adım 1)",
    "veri_kilitleme":     "STRING  → Kesin rakamlar listesi (CoT adım 2)",
    "oz_elestiri":        "STRING  → Öz denetim (CoT adım 3)",
    "seslendirme_metni":  "STRING  → TTS metni (hedef kelime ±%10)",
    "reels_aciklamasi":   "STRING  → 5 katmanlı caption (hashtagsiz)",
    "reels_hashtagleri":  "ARRAY<STRING> → Tam 5 hashtag",
    "kapak_basliklari":   "ARRAY<{ana: STRING, alt: STRING}> → 5 alternatif"
  },
  "required": ["tüm alanlar"]
}
```

### Threads Schema:

```json
{
  "type": "OBJECT",
  "properties": {
    "threads_aciklamasi": "STRING → Max 500 karakter, hashtag yok"
  },
  "required": ["threads_aciklamasi"]
}
```

---

İşte dostum, projenin en güncel şeması bu! 🚀
